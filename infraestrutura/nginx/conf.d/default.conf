server {
  listen 80;
  server_name meubolso.fsconnect.tech;

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    return 301 https://$host$request_uri;
  }
}

server {
  listen 443 ssl;
  server_name meubolso.fsconnect.tech;

  ssl_certificate /etc/letsencrypt/live/meubolso.fsconnect.tech/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/meubolso.fsconnect.tech/privkey.pem;
  ssl_session_timeout 1d;
  ssl_session_cache shared:SSL:10m;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_prefer_server_ciphers off;

  location /api/ {
    proxy_pass http://backend:3000/api/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  location / {
    return 200 "Meu Bolso reverse proxy online\n";
    add_header Content-Type text/plain;
  }
}
